# === Backend ===
OPENAI_API_KEY=sk-... # required
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-large
QDRANT_COLLECTION=company_knowledge

# === (Docker-only) ===
# QDRANT_URL is set to http://qdrant:6333 inside docker-compose network
# For local dev without Docker, set QDRANT_URL=http://localhost:6333

# === Frontend ===
# For local dev: frontend/.env with VITE_API_BASE=http://localhost:5088
